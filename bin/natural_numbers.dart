import 'dart:io';

void main() {
  int naturalNumber = 0;
  int sum = 0;
  for (naturalNumber=0;naturalNumber <=7;naturalNumber++) {
    sum+=naturalNumber;
  }
  print("The Sum of Natural Number up to 7 terms is $sum");
}